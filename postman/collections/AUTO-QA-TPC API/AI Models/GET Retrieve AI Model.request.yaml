$kind: http-request
name: GET Retrieve AI Model
method: GET
url: '{{baseUrl}}/api/core/ai-models/{{modelId}}/'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
