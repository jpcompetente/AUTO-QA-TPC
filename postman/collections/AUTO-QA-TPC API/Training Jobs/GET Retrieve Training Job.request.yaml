$kind: http-request
name: GET Retrieve Training Job
method: GET
url: '{{baseUrl}}/api/core/training-jobs/{{jobId}}/'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
