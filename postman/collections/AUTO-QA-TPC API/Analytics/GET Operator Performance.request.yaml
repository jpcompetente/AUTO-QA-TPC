$kind: http-request
name: GET Operator Performance
method: GET
url: '{{baseUrl}}/api/core/analytics/operator-performance/'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
