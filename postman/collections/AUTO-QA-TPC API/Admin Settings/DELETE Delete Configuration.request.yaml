$kind: http-request
name: DELETE Delete Configuration
method: DELETE
url: '{{baseUrl}}/api/core/admin-settings/{{configId}}/'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
