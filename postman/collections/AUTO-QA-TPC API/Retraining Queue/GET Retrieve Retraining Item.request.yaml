$kind: http-request
name: GET Retrieve Retraining Item
method: GET
url: '{{baseUrl}}/api/core/retraining-queue/{{queueId}}/'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
