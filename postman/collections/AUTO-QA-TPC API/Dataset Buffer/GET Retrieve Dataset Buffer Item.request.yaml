$kind: http-request
name: GET Retrieve Dataset Buffer Item
method: GET
url: '{{baseUrl}}/api/core/dataset-buffer/{{bufferId}}/'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
