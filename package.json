{
  "name": "auto-qa-tpc",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend-vite run dev",
    "build": "npm --prefix frontend-vite run build",
    "lint": "npm --prefix frontend-vite run lint",
    "preview": "npm --prefix frontend-vite run preview"
  }
}
